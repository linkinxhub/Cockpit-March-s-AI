import{assets}from'@/lib/market-data';
import{authorizeApiRequest}from'@/lib/access-control';
import{getUserSyncStore,userSyncStoreConfigured}from'@/lib/user-sync-store';
import{readBoundedJson,RequestSecurityError}from'@/lib/request-security';
const valid:Set<string>=new Set(assets.map(a=>a.key as string));
export async function PUT(req:Request){const auth=await authorizeApiRequest();if(auth.response)return auth.response;const user=auth.context.identity;if(!userSyncStoreConfigured())return Response.json({error:'storage_not_configured'},{status:503});let body:any;try{body=await readBoundedJson(req,16_000)}catch(e){return Response.json({error:e instanceof RequestSecurityError?e.message:'invalid_json'},{status:e instanceof RequestSecurityError?e.status:400})}const rawKeys:Array<unknown>=Array.isArray(body?.assetKeys)?body.assetKeys:[];const assetKeys:string[]=[...new Set(rawKeys.filter((x):x is string=>typeof x==='string'&&valid.has(x)))];if(!Array.isArray(body?.assetKeys))return Response.json({error:'assetKeys_required'},{status:400});try{await getUserSyncStore().setWatchlist(user.id,assetKeys);return Response.json({ok:true,watchlist:assetKeys},{headers:{'Cache-Control':'private, no-store'}})}catch{return Response.json({error:'storage_unavailable'},{status:503})}}
