import{getSharedUserIdentity}from'@/lib/user-identity';
import{defaultNotificationPreferences,getUserSyncStore,userSyncStoreConfigured,type NotificationPreferences,type NotificationSeverity}from'@/lib/user-sync-store';
const severities=new Set<NotificationSeverity>(['INFO','IMPORTANT','CRITIQUE']);
export async function PUT(req:Request){const user=await getSharedUserIdentity();if(!user)return Response.json({error:'authentication_required'},{status:401});if(!userSyncStoreConfigured())return Response.json({error:'storage_not_configured'},{status:503});let body:any;try{body=await req.json()}catch{return Response.json({error:'invalid_json'},{status:400})}const minimumSeverity=severities.has(body?.minimumSeverity)?body.minimumSeverity:defaultNotificationPreferences.minimumSeverity;const value:NotificationPreferences={minimumSeverity,watchedOnly:body?.watchedOnly!==false,pushEnabled:body?.pushEnabled===true,quietHoursStart:typeof body?.quietHoursStart==='string'?body.quietHoursStart:null,quietHoursEnd:typeof body?.quietHoursEnd==='string'?body.quietHoursEnd:null};try{await getUserSyncStore().setNotificationPreferences(user.id,value);return Response.json({ok:true,notificationPreferences:value},{headers:{'Cache-Control':'private, no-store'}})}catch{return Response.json({error:'storage_unavailable'},{status:503})}}
