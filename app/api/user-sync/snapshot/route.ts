import{authorizeApiRequest}from'@/lib/access-control';
import{getUserSyncStore,userSyncStoreConfigured}from'@/lib/user-sync-store';
export async function GET(){const auth=await authorizeApiRequest();if(auth.response)return auth.response;const user=auth.context.identity;if(!userSyncStoreConfigured())return Response.json({error:'storage_not_configured',configured:false},{status:503});try{const snapshot=await getUserSyncStore().getSnapshot(user.id);return Response.json({user:{id:user.id,displayName:user.displayName},snapshot,configured:true},{headers:{'Cache-Control':'private, no-store'}})}catch{return Response.json({error:'storage_unavailable'},{status:503})}}
