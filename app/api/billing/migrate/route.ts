import{runBillingMigration}from'@/lib/billing-migration-runner';
const expectedBranch='feat/precommercialization-audit';const expectedConfirmation='APPLY_BILLING_V1';
export async function POST(req:Request){if(process.env.VERCEL_ENV!=='preview')return Response.json({error:'preview_only'},{status:403});if(process.env.VERCEL_GIT_COMMIT_REF!==expectedBranch)return Response.json({error:'wrong_branch'},{status:403});const url=new URL(req.url);if(url.searchParams.get('confirm')!==expectedConfirmation)return Response.json({error:'confirmation_required'},{status:400});try{const result=await runBillingMigration();return Response.json({ok:true,changed:result.changed,healthy:result.after.healthy},{headers:{'Cache-Control':'no-store','X-Robots-Tag':'noindex'}});}catch(e){return Response.json({error:e instanceof Error?e.message:'billing_migration_failed'},{status:500,headers:{'Cache-Control':'no-store','X-Robots-Tag':'noindex'}});}}
