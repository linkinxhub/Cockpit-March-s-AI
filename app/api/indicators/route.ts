import{assets,chartPeriods,history,timeframeSnapshot,type ChartPeriod}from'@/lib/market-data';import{buildIndicatorCenter}from'@/lib/indicator-engine';export async function GET(req:Request){const url=new URL(req.url),key=url.searchParams.get('symbol')||'',period=(url.searchParams.get('period')||'1d')as ChartPeriod;if(!assets.some(a=>a.key===key))return Response.json({error:'Symbole invalide'},{status:400});if(!(period in chartPeriods))return Response.json({error:'Période invalide'},{status:400});try{const points=await history(key,period),analysis=timeframeSnapshot(key,points,period),center=buildIndicatorCenter(analysis.points,{ema20:analysis.ema20,ema50:analysis.ema50,rsi:analysis.rsi,support:analysis.support,resistance:analysis.resistance,last:analysis.last});return Response.json({symbol:key,period,...center,updatedAt:new Date().toISOString()},{headers:{'Cache-Control':'public, max-age=60'}})}catch{return Response.json({error:'Indicateurs indisponibles'},{status:503})}}