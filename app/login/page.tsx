'use client';
import{FormEvent,useState}from'react';

export default function LoginPage(){const[mode,setMode]=useState<'login'|'register'>('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[displayName,setDisplayName]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password,displayName})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Connexion impossible');location.href='/mobile-connect';}catch(e){setError(e instanceof Error?e.message:'Erreur');}finally{setBusy(false)}}return <main style={{maxWidth:460,margin:'48px auto',padding:24,fontFamily:'system-ui'}}><p style={{fontWeight:800,letterSpacing:1}}>COCKPIT MARCHÉS AI</p><h1>{mode==='login'?'Se connecter':'Créer un compte'}</h1><p>Ce compte Web permet d’utiliser Cockpit dans Chrome et d’appairer votre téléphone en toute sécurité.</p><form onSubmit={submit} style={{display:'grid',gap:14}}>{mode==='register'&&<label>Nom affiché<input value={displayName} onChange={e=>setDisplayName(e.target.value)} required style={{display:'block',width:'100%',padding:12,marginTop:6}}/></label>}<label>Email<input type='email' value={email} onChange={e=>setEmail(e.target.value)} autoComplete='email' required style={{display:'block',width:'100%',padding:12,marginTop:6}}/></label><label>Mot de passe<input type='password' value={password} onChange={e=>setPassword(e.target.value)} autoComplete={mode==='login'?'current-password':'new-password'} minLength={10} required style={{display:'block',width:'100%',padding:12,marginTop:6}}/></label><button disabled={busy} style={{padding:14,fontWeight:700}}>{busy?'Patientez…':mode==='login'?'Se connecter':'Créer mon compte'}</button></form>{error&&<p role='alert' style={{color:'crimson'}}>{error}</p>}<button onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}} style={{marginTop:18,padding:10}}>{mode==='login'?'Je n’ai pas encore de compte':'J’ai déjà un compte'}</button><p style={{marginTop:24,fontSize:13}}>Mot de passe minimum : 10 caractères. La session est conservée dans un cookie HTTP-only sécurisé.</p></main>}
