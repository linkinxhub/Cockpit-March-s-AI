import Link from'next/link';import{redirect}from'next/navigation';import{auth0SignUpPath}from'@/app/chatgpt-auth';
export default async function SignUpPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){if(!process.env.AUTH0_DOMAIN||!process.env.AUTH0_CLIENT_ID||!process.env.AUTH0_CLIENT_SECRET||!process.env.AUTH0_SECRET)return <main className="authShell"><section><h1>Inscription temporairement indisponible</h1><Link href="/">Retour au cockpit</Link></section></main>;const params=await searchParams;const target=params.returnTo??params.redirect_url;redirect(auth0SignUpPath(typeof target==='string'?target:'/account'))}
