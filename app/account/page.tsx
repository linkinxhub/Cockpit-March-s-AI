'use client';
import{useEffect,useState}from'react';
export default function AccountPage(){const[billing,setBilling]=useState<any>(null),[confirm,setConfirm]=useState(''),[message,setMessage]=useState('');useEffect(()=>{fetch('/api/billing/status',{cache:'no-store'}).then(async r=>r.ok?r.json():null).then(setBilling).catch(()=>{});},[]);async function portal(){const r=await fetch('/api/billing/portal',{method:'POST'});const d=await r.json();if(d.url)location.href=d.url;else setMessage(d.error||'Portail indisponible.');}async function erase(){setMessage('');const r=await fetch('/api/account/delete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirm})});const d=await r.json();if(r.ok)setMessage('Données applicatives supprimées. Déconnectez-vous puis fermez cette page.');else setMessage(d.error||'Suppression impossible.');}return <main style={{maxWidth:800,margin:'0 auto',padding:'48px 20px',fontFamily:'system-ui',lineHeight:1.6}}><h1>Mon compte</h1><section><h2>Abonnement</h2><p>{billing?<>Plan <b>{billing.plan}</b> · statut <b>{billing.status}</b></>:'Chargement…'}</p>{billing?.hasCustomer&&<button onClick={portal}>Gérer la facturation</button>}</section><section style={{marginTop:32}}><h2>Mes données</h2><p><a href='/api/account/export'>Télécharger mes données au format JSON</a></p></section><section style={{marginTop:32,border:'1px solid #d99',borderRadius:12,padding:20}}><h2>Supprimer mon compte</h2><p>Cette action efface les données applicatives synchronisées. Un abonnement payant actif doit d’abord être annulé. Certaines pièces comptables peuvent rester conservées chez le prestataire de paiement lorsque la loi l’exige.</p><label>Pour confirmer, saisissez <b>DELETE_MY_ACCOUNT</b><br/><input value={confirm} onChange={e=>setConfirm(e.target.value)} style={{width:'100%',maxWidth:380,padding:10,marginTop:8}}/></label><br/><button onClick={erase} disabled={confirm!=='DELETE_MY_ACCOUNT'} style={{marginTop:12}}>Supprimer définitivement</button></section>{message&&<p role='alert'>{message}</p>}<p style={{marginTop:32}}><a href='/pricing'>Offres</a> · <a href='/legal/privacy'>Confidentialité</a></p></main>}
