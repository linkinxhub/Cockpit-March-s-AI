'use client';
import{useState}from'react';
import type{AccountMembership,SupportedLocale}from'@/lib/account-types';
import{SUPPORTED_LOCALES}from'@/lib/account-types';
import{accountCopy}from'@/lib/account-copy';
export function ProfileForm({account,copy}:{account:AccountMembership;copy:ReturnType<typeof accountCopy>}){const[displayName,setDisplayName]=useState(account.displayName),[locale,setLocale]=useState<SupportedLocale>(account.locale),[avatarUrl,setAvatarUrl]=useState(account.avatarUrl||''),[state,setState]=useState<'idle'|'saving'|'saved'|'error'>('idle');async function submit(e:React.FormEvent){e.preventDefault();setState('saving');const response=await fetch('/api/account/profile',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({displayName,locale,avatarUrl:avatarUrl||null})});setState(response.ok?'saved':'error');}return <form className="accountForm" onSubmit={submit}><label>{copy.name}<input value={displayName} onChange={e=>setDisplayName(e.target.value)} minLength={2} maxLength={80} required/></label><label>{copy.email}<input value={account.email} disabled/></label><label>{copy.language}<select value={locale} onChange={e=>setLocale(e.target.value as SupportedLocale)}>{SUPPORTED_LOCALES.map(value=><option key={value} value={value}>{value.toUpperCase()}</option>)}</select></label><label>{copy.avatar}<input type="url" value={avatarUrl} onChange={e=>setAvatarUrl(e.target.value)} maxLength={500}/></label><button disabled={state==='saving'}>{state==='saving'?copy.saving:copy.save}</button>{state==='saved'?<p className="accountSuccess">{copy.saved}</p>:null}{state==='error'?<p className="accountError">{copy.error}</p>:null}</form>}
