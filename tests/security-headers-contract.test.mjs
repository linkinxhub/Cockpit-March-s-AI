import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';
test('global security headers are explicit and Stripe-ready',()=>{const config=fs.readFileSync('next.config.ts','utf8');for(const header of['Content-Security-Policy','Strict-Transport-Security','X-Content-Type-Options','Referrer-Policy','Permissions-Policy','X-Frame-Options'])assert.match(config,new RegExp(header));assert.match(config,/stripe\.com/);assert.doesNotMatch(config,/default-src \*/);});
test('premium market responses are never publicly cached',()=>{for(const file of['app/api/bigdata/route.ts','app/api/ichimoku-history/route.ts']){const source=fs.readFileSync(file,'utf8');assert.doesNotMatch(source,/Cache-Control':'public/);assert.match(source,/Cache-Control':'private/);}});
