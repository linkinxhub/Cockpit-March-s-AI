const {test}=require('node:test'),assert=require('node:assert/strict'),ts=require('typescript'),fs=require('fs');
const m={exports:{}};new Function('module','exports',ts.transpileModule(fs.readFileSync('lib/pack-action.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(m,m.exports);const action=m.exports.packAction;
for(const plan of ['free','DISCOVERY','pro','PRO','expert','EXPERT','trader_plus'])test('correct actions for '+plan,()=>{const current=['free','DISCOVERY'].includes(plan)?'free':['pro','PRO'].includes(plan)?'pro':'expert';const user={plan,status:'active',suspended:false,hasSubscription:current!=='free',hasCustomer:current!=='free'};for(const target of ['free','pro','expert'])assert.equal(action(target,user),target===current?'current':current!=='free'?'portal':'checkout');});
test('anonymous, suspended, past due and canceled states',()=>{assert.equal(action('free',null),'account');assert.equal(action('pro',null),'checkout');assert.equal(action('pro',{plan:'pro',status:'active',suspended:true}),'blocked');assert.equal(action('pro',{plan:'pro',status:'past_due',hasSubscription:true,hasCustomer:true}),'portal');assert.equal(action('free',{plan:'pro',status:'canceled',hasSubscription:true,hasCustomer:true}),'current');});
